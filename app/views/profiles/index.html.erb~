<strong> This is profile page</strong><br><a data-toggle="modal" href="#myModal" class="btn btn-primary">Send a message</a><br>

<%=@user.first_name%><br>
<%=@user.last_name%><br>
<%=@user.gender%><br>
<%=@user.link%><br>
<%=@user.verified%><br>
<%=@user.date_of_birth%><br>
<%=@user.current_location%><br>
<%=@user.hometown%>
<br>

<div  class= "row" style ="background-color: #049cbd;"><b>My rides</b></div>
<br>
<%@user.user.posts.each do |ride|%>
From: <%=ride.start.address%>
To: <%=ride.finish.address%><br>
<%= link_to 'Edit', edit_post_path(ride), :class => 'btn' %>
  <%= link_to 'Delete', post_path(ride), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
<%end%>
<b>Inbox</b>
<% @user.mailbox.inbox.each do |message|%>
<%= message.subject%><a data-toggle="modal" href=#message class="btn btn-primary">show</a><br>
          <div class="modal hide fade" id=message>
            <div class="modal-header">
              <a class="close" data-dismiss="modal">×</a>
              <h3><%= message.subject%></h3>
            </div>
            <div class="modal-body">
              <p>
                <%=message.receipts.last.message.body%></p>
            </div>
            <div class="modal-footer">
              <a href="#" class="btn", data-dismiss="modal">Close</a>
              <a href="#" class="btn btn-primary">Reply</a>
            </div>
          </div>
    
<%end%><br>
<b>Sent Item</b>
<% @user.mailbox.sentbox.each do |message|%>
<%= message.subject %>
<%end%><br>

<br>
trash

<% @user.mailbox.trash.each do |message|%>
<%= message.subject%>
<%end%>

<div class="modal hide fade" id="myModal">
  <div class="modal-header">
    <a class="close" data-dismiss="modal">×</a>
    <h3>Send Messager</h3>
  </div>
  <div class="modal-body">
    <p><%=form_tag send_message_user_profiles_path, method: :get do %>
         
    <%= text_area_tag :body, params[:body], class:"input-xxlarge", placeholder: "Message", rows:"7",  %><br>
    
      <div class="modal-footer">
        <a href="#" class="btn", data-dismiss="modal">Close</a>
       <%= submit_tag "Send", :name => nil, class: "btn" %>
      </div>
       <%end%>
    </p>
    
  </div>
  
</div>
<br>

