<% matches = 0 %>
<tbody>
	<%for start_location in @start_locations%>
      <% if !@end_locations.find_by_post_id(start_location.post.id).nil? %>
		   <tr>
        <td><%= start_location.post.id %></td>
        <td><%= link_to start_location.post.starttime, post_path(start_location.post) %></td>
        <td><%= start_location.address %></td>
        <td><%= start_location.post.finish.address %></td>
        <td>
          <%= link_to 'Edit', edit_post_path(start_location.post), :class => 'btn btn-mini' %>
          <%= link_to 'Destroy', post_path(start_location.post), :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
					<% matches = matches + 1%>
      <%end%>
      <% if !@stops2.find_by_post_id(start_location.post.id).nil? %>
		   <tr>
        <td><%= start_location.post.id %></td>
        <td><%= link_to start_location.post.starttime, post_path(start_location.post) %></td>
        <td><%= start_location.address %></td>
        <td><%= start_location.post.finish.address %></td>
        <td>
          <%= link_to 'Edit', edit_post_path(start_location.post), :class => 'btn btn-mini' %>
          <%= link_to 'Destroy', post_path(start_location.post), :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
					<% matches = matches + 1%>
			<%end%>
		<%end%>
    <%for stops1 in @stops1%>
          <% if !@end_locations.find_by_post_id(stops1.post.id).nil? %>
		          <%post= stops1.post%>
					      <tr>
        <td><%= post.id %></td>
        <td><%= link_to post.starttime, post_path(post) %></td>
        <td><%= post.start.address %></td>
        <td><%= post.finish.address %></td>
        <td>
          <%= link_to 'Edit', edit_post_path(post), :class => 'btn btn-mini' %>
          <%= link_to 'Destroy', post_path(post), :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
					    <% matches = matches + 1%>
          <%end%>
          <% if !@stops2.find_by_post_id(stops1.post.id).nil? %>
		
					    <%post= stops1.post%>
					      <tr>
        <td><%= post.id %></td>
        <td><%= link_to post.starttime, post_path(post) %></td>
        <td><%= post.start.address %></td>
        <td><%= post.finish.address %></td>
        <td>
          <%= link_to 'Edit', edit_post_path(post), :class => 'btn btn-mini' %>
          <%= link_to 'Destroy', post_path(post), :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
					    <% matches = matches + 1%>
			    <%end%>
		 <%end%>
		<%="No matches" if matches == 0%>
 	</tbody>
